name: Keep Apps Awake

on:
  schedule:
    - cron: '0 */5 * * *'
  workflow_dispatch: 

jobs:
  ping_app:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4  # No 'with' section here

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11' # 'with' belongs here

      - name: Install dependencies
        run: |
          pip install selenium webdriver-manager

      - name: Run keep_awake script
        run: python keep_awake.py
